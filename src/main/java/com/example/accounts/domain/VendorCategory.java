package com.example.accounts.domain;

/** What a vendor supplies — shown on the admin approval queue and the vendor directory so procurement can filter by what they're actually shopping for. */
public enum VendorCategory {
    SUPPLIES,
    SERVICES,
    MAINTENANCE,
    LOGISTICS,
    PROFESSIONAL_SERVICES
}
