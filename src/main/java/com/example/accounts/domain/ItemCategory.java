package com.example.accounts.domain;

/** What a catalog item is — software licences, physical hardware, laptops specifically (high enough volume to track separately), IoT devices, and accessories/frills. */
public enum ItemCategory {
    SOFTWARE,
    HARDWARE,
    LAPTOP,
    IOT_DEVICE,
    ACCESSORY
}
